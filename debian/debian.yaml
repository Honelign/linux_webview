flutter_app: 
  command: webview_cool_app
  arch: x64
  parent: /usr/local/lib
  nonInteractive: false

control:
  Package: webview-cool-app
  Version: 1.0.0
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer:
  Description: webview Cool App that does everything!
  
#options:
#  exec_out_dir: debian/packages